<% if user_signed_in? %>
  <p>login succeed!!</p>
<% else %>
  <h2>Welcome to E-Lerning System</h2>
  <button class="btn btn-warning"><%= link_to "Sign Up", new_user_registration_path, class: 'post' %></button>
<% end %>